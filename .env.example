# ===================================================================
# CONFIGURAÇÃO GERAL DO JARVIS
# ===================================================================
# Nível de log da aplicação (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Palavra de ativação para o cliente
WAKE_WORD=jarvis

# ===================================================================
# CHAVES DE API E SERVIÇOS EXTERNOS
# ===================================================================
# Chave de acesso OBRIGATÓRIA para o Picovoice Porcupine (wake word).
# Obtenha a sua em: https://console.picovoice.ai/
PORCUPINE__ACCESS_KEY="YOUR_PICOVOICE_ACCESS_KEY"

# ===================================================================
# CONFIGURAÇÃO DO BANCO DE DADOS POSTGRESQL
# ===================================================================
# Estas variáveis são usadas tanto pelo Pydantic quanto pelo docker-compose.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=jarvis
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ===================================================================
# CONFIGURAÇÃO DO NEO4J (Banco de Dados de Grafos)
# ===================================================================
# Credenciais para o serviço Neo4j.
# A senha aqui deve ser a mesma usada na variável NEO4J_AUTH no docker-compose.yml.
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# ===================================================================
# CONFIGURAÇÃO DOS MODELOS DE IA (LLM, STT, TTS)
# ===================================================================
# URL base do serviço Ollama que está rodando localmente.
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Caminho para o modelo de Speech-to-Text (Vosk).
# O padrão já está configurado, mas pode ser sobrescrito aqui.
STT_MODEL_PATH=models/stt/vosk-model-small-pt-0.3

# (Opcional) Caminho para um arquivo .wav para clonagem de voz com o TTS.
# Exemplo: TTS__SPEAKER_WAV="models/tts/minha_voz.wav"
TTS__SPEAKER_WAV_PATH=""